#Tree format that is the reason we
#are using application.yml file and it is well structured

server:
  port: 8080


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/micro_db
    username: root
    password: 8767780674
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

#  application:
#    name: USER-SERVICE

# This is the property of the config server
  config:
    import: configserver:http://localhost:8084

#This is the property of the service registry/Discovery Server

#eureka:
#  instance:
#    prefer-ip-address: true
#  client:
#    fetch-registry: true
#    register-with-eureka: true
#    service-url:
#        defaultZone: http://localhost:8761/eureka


# This is the properties of the circuit breaker
management:
  health:
    circuitbreaker:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health


  endpoint:
    health:
      show-details: always

resilience4j:
  circuitbreaker:
    instances:
      ratingHotelBreaker:
        registerHealthIndicator: true
        eventConsumerBufferSize: 10
        failureRateThreshold: 50
        minimumNumberOfCalls: 5
        automaticTransitionFromOpenToHalfOpenEnabled: true
        waitDurationInOpenState: 6s
        permittedNumberOfCallsInHalfOpenState: 3
        slidingWindowSize: 10
        slidingWindowType: COUNT_BASED
